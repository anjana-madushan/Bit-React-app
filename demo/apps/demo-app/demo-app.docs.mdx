---
description: 'A demo-app app.'
labels: ['app', 'react']
---

A React application for demo-app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use demo-app 
```

To run the app locally, run the following:

```
bit run demo-app 
```
